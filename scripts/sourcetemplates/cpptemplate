#include "${CLASSNAME}.h"
//std inc
#include <iostream>

using namespace ${PROJECTNAME};
using namespace ${NAMESPACE};

std::ostream& ${PROJECTNAME}::${NAMESPACE}::operator<<(std::ostream& s, const ${NAMESPACE}::${CLASSNAME}& a)
{
    s.clear();
    s << "{" 
    << "${CLASSNAME}'s operator<< has not been implemented" 
    << "}";
    return s;

}